{
  "note": "Phase4ZBでは新規生成は最小限(既存Phase4Z/4ZAデータの再利用を優先)。参考として、Phase4ZAで確立したllama-server経由のgreedy設定を記録する。",
  "llama_server_request_body_example": {
    "messages": "[{role: system, content: <system.jinja2>}, {role: user, content: <question>}]",
    "temperature": 0.0,
    "n_predict": 300
  },
  "hf_pytorch_generation_example": {
    "max_new_tokens": 300,
    "do_sample": false,
    "temperature": "n/a (greedy)"
  },
  "consistency_with_phase4z_4za": "temperature=0.0(greedy)、max_tokens/n_predict=300、system prompt/chat templateはPhase4Z/4ZAと完全同一。Phase4ZBでは重み比較(Section7-11)が主診断であり、新規のtext generationはPhase4ZA既存データの参照のみで、追加のgreedy生成は実施していない。"
}
